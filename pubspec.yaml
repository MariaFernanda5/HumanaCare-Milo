name: humanacare_paciente
description: HumanaCare — Fluxo do Paciente (mock, sem Firebase)
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^4.14.0
  google_fonts: ^6.1.0
  go_router: ^13.2.0
  provider: ^6.1.2
  intl: ^0.19.0
  http: ^1.2.0
  firebase_auth: ^6.6.1
  cloud_firestore: ^6.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
